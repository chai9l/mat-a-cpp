#include <string>
#include <iostream>
#include <stdexcept>
#include <vector>
#include "mat.hpp"

using namespace std;

const vector<vector<char>> mat_vector;

namespace ariel {
    string mat(int row, int coll, char pattern, char pattern2) {
        
        // if (row%2 == 0 || coll%2 == 0) { throw exception ex
        // }

        
        return "";
    }
}